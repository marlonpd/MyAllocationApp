class Budget {
  constructor(id, budgetId, userId, name, amount) {
    this.id = id
    this.budgetId = budgetId
    this.userId = userId
    this.name = name
    this.amount = amount
  }
}

export default Budget
